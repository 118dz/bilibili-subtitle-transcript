# 更新日志

## 1.2.2

- 优化扩展更新后的错误提示：如果 Chrome 提示 `Extension context invalidated`，会明确提示刷新当前 B 站视频页面一次。

## 1.2.1

- 修复 B 站站内切换视频后，插件面板仍停留在旧页面状态、需要刷新才能读取的问题。
- 切换视频时会重新记录播放器实际加载的字幕资源；手动打开中文字幕后，面板会自动重读当前视频。
- 当页面已经显示中文字幕但资源监听漏掉时，只采用能匹配当前画面字幕文本的字幕源。

## 1.2.0

- CLI 默认改为浏览器抓取模式，自动打开本机 Chrome 并监听播放器实际加载的字幕资源。
- 新增 `--headed`、`--browser-timeout`、`--browser-channel`。
- `--allow-api` 保留为显式接口模式。

## 1.1.0

- 新增命令行字幕下载工具 `scripts/download-subtitle.mjs`。
- 支持给定 B 站 URL 直接导出 TXT、SRT 和 JSON。
- 支持通过 `--cookie`、`--cookie-file` 或 `BILI_COOKIE` 读取登录态字幕。
- 支持 `--subtitle-url` 直接下载页面插件实际使用的字幕源。
- 页面插件新增「命令」按钮，可复制本地下载命令。
- 页面播放器没有字幕入口时，插件直接提示无字幕，不再使用接口或历史资源里的其他字幕。
- CLI 默认不再使用接口字幕，避免和播放器实际字幕错配；需要接口模式时显式传 `--allow-api`。
- 页面插件只读取播放器实际加载的字幕资源，不再用接口字幕兜底，避免无字幕视频输出错配内容。

## 1.0.0

- 支持读取 B 站 AI 字幕和人工字幕。
- 支持优先匹配播放器当前实际加载的字幕资源。
- 支持复制文字稿、导出 TXT、导出 SRT。
- 支持页面内快捷面板和扩展弹窗。
- 支持字幕质量检测，识别无效字幕轨并提示 OCR/语音识别方案。
